<UserControl x:Class="MsPathViewer.Views.SpectrumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.codeplex.com"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*" />
            <RowDefinition Height="50*" />
        </Grid.RowDefinitions> 
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75*" />
            <ColumnDefinition Width="25*" />
        </Grid.ColumnDefinitions>
        <oxy:Plot Name="Ms2Spectrum" DataContext="{Binding Ms2SpectrumViewModel}" Model="{Binding Path=Plot}" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" />
        <oxy:Plot Name="PreviousMs1" DataContext="{Binding PreviousMs1ViewModel}" Model="{Binding Path=Plot}" Grid.Row="0" Grid.Column="1" />
        <oxy:Plot Name="NextMs1" DataContext="{Binding NextMs1ViewModel}" Model="{Binding Path=Plot}" Grid.Row="1" Grid.Column="1" />
    </Grid>
</UserControl>
